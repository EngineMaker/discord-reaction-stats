# Discord リアクション集計ツール

Discord ギルドのリアクションを「もらった数 / 付けた数」でメンバー別・リアクション別に集計し、自己完結の HTML レポートを出力する個人ツール。使い方は `README.md` を参照。

## 現在の状況 (2026-07-22 時点)

ローカルの個人ツールとしては**一通り動いている**。2026-06 / 2026-07 の実データを収集済み（リアクション約3800件、投稿約4400件、660チャンネル/スレッド走査）。ロール取得・投稿数集計・フォーラム対応・合計タブ・20位→合計32のカットオフ・戻るボタン・合計デフォルトまで実装・検証済み。

**次の大きな節目は「クラウド運用への移行」で、方針が固まったが未着手。** 下記参照。

### 次にやること: クラウド移行（順序厳守）
2026-07-22 に設計確定。**この順で進める。同時に変えると問題の切り分けができなくなる。**

1. **TypeScript 化** — ✅ **2026-07-23 完了。** src の5ファイルと test を `.ts` 化。`tsx` を devDependency に入れ、`node --import tsx src/xxx.ts` で実行（ビルド不要）。型チェックは `npm run typecheck` (= `tsc --noEmit`)。行の型は `db.ts` に集約 (`UserRow` 等) し `.all() as XxxRow[]` で受ける。`tsconfig` は今 `strict:false`（段階的に締める余地あり。次は strictNullChecks）。`report.ts` の HTML内埋め込みJS はテンプレート文字列のまま（型の恩恵薄いので触らない）。**検証: `npm test` 全項目パス / スナップDBからのレポート生成が旧版と同一（合計デフォルト・カットオフ・戻る・EM住民フィルタ全て動作、JSエラーなし）。ロジックは一切変えていない。**
2. **Neon (Postgres) 化** — 主作業は `INSERT OR IGNORE` → `ON CONFLICT DO NOTHING` の置換。`upsertMember` の `ON CONFLICT DO UPDATE` はほぼそのまま
3. **GitHub Actions で月イチ集計** — トークンは Secrets。cron は UTC（JST6時=UTC21時・前日）。schedule遅延あり / 60日コミット無しで自動無効化（`workflow_dispatch` 併記）/ 重いので1時間超なら `scan_state` で分割
4. **Cloudflare Pages + Discord OAuth 閲覧ページ** — 最後。スコープ `identify`+`guilds` のみ。**対象サーバーのメンバー全員が閲覧可**（ロール絞りなし）。メンバー確認が済むまでデータを配信しないこと（サーバー側判定）。client secret とセッション署名鍵は Cloudflare の環境変数へ

**却下した案**: CockroachDB（分散はこの規模で過剰）/ クエリ文字列パスワード（データが手元にある時点でザル）/ GitHub・Google 認証（メンバーの該当アカウントを把握できない）。

**ドメイン**: **`emaker.dev`**（EngineMaker の略）。このツールはシェアハウス EngineMaker 全体のものという位置づけ。Cloudflare Registrar で取得予定（`.dev`は直接取得可・原価・NS設定不要）。`.dev` はHTTPS必須だが Pages が証明書自動発行。閲覧ページは `reactions.emaker.dev` 等のサブドメインを想定。取得はユーザー操作なので代行不可。

git はまだ未初期化。移行の前に `git init` しておくと、TypeScript化の差分が追える。

## 設計上の判断（変更する前に読むこと）

### 集計期間は「JST 6時始まりの月」
ユーザーの明示的な要件。`src/period.ts` に閉じ込めてある。UTC+9 固定（日本に夏時間なし）なのでオフセット計算だけで完結。**境界値のユニットテストは書いたが、テストファイルとしては残していない** — 触るなら 6/1 5:59→前月、6/1 6:00→当月、年またぎ (1/1 5:00→前年12月) を最低限確認すること。

### 履歴スキャン方式を選んだ（常駐 Bot ではなく）
ユーザーが「とりあえず 2026年6月分が見たい」と言ったため。**運用を始めたら常駐 Bot も検討したいとのこと。**

この方式の根本的な制約: **取り消されたリアクションは復元できない。** スキャン時点で残っているものしか見えない。この制約はユーザーに説明済みで、了解のうえで進めている。正確に追うには `messageReactionAdd`/`Remove` を購読する常駐 Bot が必要。その場合も既存の `reactions` テーブルにそのまま書けるので、レポート側は変更不要。

### EM住民ロールは 0 件でもランキングに載せる
ユーザーの明示的な要件。対象ロール名は `src/report.ts` の `FOCUS_ROLE` 定数 (`'EM住民'`) 一箇所で定義している。ロールを増やしたくなったらここを配列化する。

- ロールは **`scan` の冒頭で毎回 `guild.members.fetch()` して取り直す**。走査済みチャンネルが全部 skip される再実行でも必ず通るので、ロールの付け外しは次回スキャンで反映される
- `users.roles` はロール**名**の JSON 配列。ID ではなく名前で持っているのは、レポート側が名前で絞り込むため
- `is_member` で現メンバーか退会者かを区別する。スキャンのたびに全員 0 にリセットしてから付け直すので、**退会者はランキングから自動的に消える**（リアクションの記録自体は消さない）
- リアクション経由の `upsertUser` はロール列を触らない。`upsertMember` と役割が違うので、統合しようとしないこと

レポート側の絞り込みは**表示だけの操作**で、件数そのものは絞り込み前の実データのまま。ただし KPI と絵文字内訳は絞り込みに追随させている（表だけ絞られて上の数字が全体のままだと誤読を招くため）。メンバーが並ぶ表は全部 `memberEntries()` を通すこと。

### 表示名はサーバーのニックネームを優先する
ユーザーの要望。`msg.author.displayName` は**グローバル表示名でありサーバーのニックネームではない**ため、リアクション経由で拾った名前は分かりにくいことがある。

- メンバー一覧側 (`upsertMember`) が正: `m.nickname ?? m.user.globalName ?? m.user.username`
- リアクション経由 (`upsertUser`) は `is_member = 1` の人の `display_name` を**上書きしない**（`CASE WHEN` でガード）。これが無いと、メンバー一覧で入れた正しい名前が後から潰される
- **表示名は DB に保存済みの値なので、直すには再スキャンが必要**

### フォーラム/スレッドも走査する（一度まるごと漏らした）
**フォーラムチャンネル自体はメッセージを持たず、中の各スレッドが実体。** 当初 `GuildText` と `GuildAnnouncement` だけを対象にしていたため、フォーラムの投稿が丸ごと集計から漏れていた（ユーザーの指摘で発覚）。

- 走査対象は「テキスト/アナウンス本体」+「全チャンネル配下のスレッド」。**フォーラム/メディアの器そのものは対象に入れない**（メッセージが無い）
- **アーカイブ済みスレッドを必ず取ること。** フォーラムの投稿は一定期間で自動アーカイブされるので、アクティブだけ見ると過去分がほとんど拾えない
- `fetchArchived` は 100件上限。**`hasMore` を見て `before` でページングする**。しないと大きいフォーラムで静かに取りこぼす
- スレッド一覧の取得は権限次第で失敗する。そのチャンネルだけ諦めて続行する（全体を止めない）
- `scan_state` はチャンネルIDで進捗を持つので、スレッドもそのまま再開の対象になる

**この収集ロジックは偽の Discord API に対してテスト済み** (`test/threads.test.ts` 相当): 器の除外、アーカイブ250件のページング、重複排除、権限の無いスレッドの除外、取得失敗時の継続を確認した。変更したら同等の検証をやり直すこと。

### 投稿数を集計している
「リアクションの有無に関わらず全メッセージをカウント」がユーザーの選択。`messages` テーブル（1メッセージ1行）に記録する。

- `scan.ts` では**リアクションの有無を判定する前**に記録すること。`msg.reactions.cache.size === 0` の `continue` より後ろに置くと無反応の投稿が数えられない
- `pending` 配列の種別は `user` / `message` / `reaction` の3つ。コミット時の分岐を漏らさないこと
- レポート側は `messages` が空なら投稿数のタブ・列・KPI ごと出さない (`hasPosts`)
- 個人詳細の「投稿あたり」は投稿0件のとき `—`（0除算回避）

### 「合計」と 20位での打ち切り
ユーザーの明示的な要件。

- **合計 = もらった + 付けた + 投稿。** 並び替えタブとしても列としても出す。**デフォルトの並び順**でもある
- タブの初期状態は2箇所で持っている: JS の `let mode` と HTML の `aria-pressed`。**デフォルトを変えるときは両方直すこと**（片方だけだとハイライトと並び順が食い違う）
- **合計が `MIN_TOTAL` (=32) 未満の人はその月は表示しない。** フィルタON/OFF どちらでも同じしきい値
- **絶対評価であることが要件。** 当初は「EM住民の20位」という相対評価で実装したが、月ごとに基準が動くのを嫌ってユーザーが絶対評価に変更した。順位ベースに戻さないこと
- 基準値を変えたいときは `MIN_TOTAL` だけ触ればよい
- 隠した人数は表のフッタに必ず出す。黙って消すと数字が信用できなくなる

### ブラウザの戻るボタンで一覧に戻れる
詳細を開くときに `history.pushState`、`popstate` で戻す。**画面内の「← 一覧に戻る」も `history.back()` を呼ぶこと** — 直接 `detail = null` にすると履歴に詳細が残り、ブラウザの戻るで詳細に入り直してしまう。

### 自己リアクションを除外している
自分の投稿に自分で付けた分 (`giver_id = receiver_id`) は数字を盛れてしまうので `src/report.ts` の SQL で除外。**ユーザーには伝えてあるが、明示的な要件ではなくこちらの判断。** 含めたいと言われたら WHERE 句から外す。

### キーキャップ絵文字は「誰が付けたか」を取得できない（API側の制約）
`*️⃣` `0️⃣`〜`9️⃣` `#️⃣` のような、異体字セレクタ `U+FE0F` + `U+20E3` を含む合成絵文字は、Discord API の `reactions/{emoji}/users` が **400 Unknown Emoji (10014)** を返す。`reaction.count` は取れるが誰が付けたかは取得不能。**こちらのバグではないので直そうとしないこと。**

対処: その絵文字だけ握りつぶして走査を続行し、**最後に必ず「集計できなかった件数」を報告する**（黙って捨てると集計から静かに消えて数字が信用できなくなる）。10014 以外のエラーは再スローする — レート制限などを飲み込まないため。

### Bot 権限は最小限 (Permissions Integer = 66560)
「チャンネルを表示」+「メッセージ履歴を読む」のみ。**`管理者` は付けない** — チャンネル個別の権限設定を無視するため、トークン漏洩時の被害が桁違いになる。Message Content Intent も不要（本文は読まない）。必要な Privileged Intent は SERVER MEMBERS INTENT のみ。

### 再実行の安全性と再開
`reactions` の主キーが `(message_id, emoji_key, giver_id)` で `INSERT OR IGNORE` なので、同じ期間を何度スキャンしても重複しない。

さらに `scan_state` により**中断しても続きから再開できる**（ユーザーの要望）。設計上の要点:
- 進捗は `(channel_id, period_key)` 単位。`period_key` は指定期間をソートして連結したもので、6月完走後に7月を走らせても誤って skip しないようにしている
- 1バッチごとに、リアクションの書き込みとカーソル前進を**同一トランザクション**でコミット。これが崩れると取りこぼしか二重計上が発生するので、いじるときは注意
- 走査済みチャンネルは API を叩かずスキップ
- `--fresh` で進捗のみリセット（リアクションデータは消さない）

**この再開ロジックは偽の Discord API に対してテスト済み**: 「1バッチごとにクラッシュ→再開」を繰り返した結果が無停止完走と完全一致すること、再実行時の API 呼び出しがゼロになることを確認した。ロジックを変更したら同等の検証をやり直すこと。

### スキーマ変更にはマイグレーションが必要
`CREATE TABLE IF NOT EXISTS` は**既存テーブルには何も効かない**ので、列を足しても既存の `data.sqlite` は古いまま `no such column` で落ちる（実際に一度やらかした）。テーブル定義を変えたら `src/db.ts` の `migrate()` に処理を足すこと。冪等にすること。

## 構成

| ファイル | 役割 |
|---|---|
| `src/period.ts` | JST 6時始まりの月境界の計算。ロジックの心臓部 |
| `src/db.ts` | SQLite スキーマ |
| `src/scan.ts` | Discord 履歴を遡ってリアクションを収集 |
| `src/report.ts` | 自己完結 HTML を生成 |

`data.sqlite` と `report.html` は生成物で `.gitignore` 済み。

## 検証方法

実データなしで HTML を確認したいときは、合成データを SQLite に流し込んで `npm run report` すればよい。前回は Playwright でスクリーンショットを撮り、JS エラーなし・ドリルダウン動作・詳細画面の数字が一覧と一致することを確認した（Playwright は検証後にアンインストール済み。依存に残っていない）。

**HTML を変更したら、ソースを読むだけで済ませず実際にレンダリングして目視すること。**

### 合成データで検証するときの注意（一度やらかした）
実 DB のコピーに架空のロールを差し込んで検証した際、`UPDATE users SET is_member = 1` を**全員に一括実行**してしまい、ロールを持たない実在ユーザーが現メンバー扱いになった。その状態のレポートをユーザーに見せてしまい、「EM住民でない人が0件で出ている」という**実在しないバグの報告をさせてしまった**。

- 合成データは**必要な行にだけ**適用する。全行 UPDATE は影響範囲を見失う
- **ユーザーに見せるレポートは必ず実 DB から生成すること**。検証用 DB の出力を成果物として見せない
- 検証で `data.sqlite` を差し替えたら、**確認まで含めて必ず元に戻す**

## 実運用で踏んだエラー（再発時の手引き）

| エラー | 原因 | 対処 |
|---|---|---|
| `Unknown Guild (10004)` | Bot 未招待、または GUILD_ID が違う | `npm run whoami` で参加中サーバーを確認。サーバーIDは左端のサーバーアイコンを右クリックして取得（チャンネル名を右クリックするとチャンネルIDになる） |
| `Unknown Emoji (10014)` | キーキャップ絵文字（上記） | 対処済み。走査は続行し末尾で報告される |
| `no such column` | スキーマ変更にマイグレーション未対応 | 対処済み。`migrate()` に処理を追加すること |

## ユーザーについて

- 日本語でやり取りする
- Discord Bot の設定には詳しくない（Developer Portal の画面の場所から案内が必要だった）。手順は具体的に、どこをクリックするかまで書くこと
- シェルは fish
- **トークンを扱うコマンドは代理実行せず、`!` プレフィックスでユーザー自身に実行してもらっている。** この方針を維持すること
- ドキュメントの置き場所（プロジェクト内 vs `~/.claude/`）の切り分けに関心が高い。プロジェクト固有の情報を勝手にユーザーディレクトリ側へ置かないこと
